#include "esp_log.h"

static const char *TAG = "spotify_credentials";

extern "C" void spotify_credentials_placeholder(void)
{
    ESP_LOGD(
        TAG,
        "Spotify-Anmeldedaten noch nicht geladen"
    );
}